---
description: 
alwaysApply: true
---

1. 需要在关键节点写上注释和日志，方便后续排查问题。
2. 需要中英文。
3. 注释用中文，日志也是中文
4. 需要符合当前项目的代码风格和UI规范。
5. 每次修改代码后都需要测试，并重新构建应用，并重新启动新的实例。
6. 无论是UI、前端功能交互、后端功能等，都需要测试用例覆盖并完全测试。新功能开发也需要对应修改测试用例。每次都需要完全验证功能ok再交付。
7. 运行go的时候用homebrew的版本
8. 前端只负责页面样式、界面展示和交互，不负责数据处理、业务编排、SQL/DDL 拼接、数据转换、复杂状态计算等逻辑。
9. 所有数据处理、业务逻辑、数据库读写、结构变更、接口编排、数据转换与校验统一放到后端处理，前端只负责调用后端接口并消费结果。
