---
description: TablePlus AI 全局 UI 设计规范，所有界面必须严格遵守
alwaysApply: true
---

# TablePlus AI — 全局 UI 设计规范

## 一、设计令牌（CSS Custom Properties）

所有尺寸、圆角、间距、字号必须使用 `globals.css` 中的 CSS 变量，**严禁硬编码**。

### 圆角
| 令牌 | 默认值 | 适用范围 |
|------|--------|----------|
| `--radius-window` | 12px | 窗口整体 |
| `--radius-panel` | 12px | 弹窗、模态框、卡片 |
| `--radius-btn` | 7px | 所有按钮 |
| `--radius-input` | 8px | 输入框、下拉框、搜索框 |
| `--radius-menu` | 8px | 右键菜单、下拉菜单 |
| `--radius-sm` | 4px | 小元素（标签、徽章） |

### 尺寸（默认模式 / 紧凑模式）
| 令牌 | 默认 | 紧凑 | 说明 |
|------|------|------|------|
| `--size-toolbar` | 36px | 32px | 顶部工具栏高度 |
| `--size-btn` | 28px | 24px | 按钮高度 |
| `--size-btn-sm` | 24px | 22px | 小按钮高度 |
| `--size-btn-icon` | 16px | 14px | 按钮图标尺寸 |
| `--size-btn-icon-sm` | 14px | 12px | 小图标尺寸 |
| `--size-input` | 32px | 28px | 输入框高度 |
| `--size-input-sm` | 28px | 24px | 小输入框高度 |
| `--size-tab` | 28px | 26px | 标签页高度 |
| `--size-font-base` | 13px | 12px | 基础字号 |
| `--size-font-sm` | 12px | 11px | 常规字号 |
| `--size-font-xs` | 11px | 10px | 小字号 |
| `--size-font-2xs` | 10px | 9px | 超小字号 |
| `--size-gap` | 8px | 6px | 标准间距 |
| `--size-gap-sm` | 4px | 2px | 小间距 |
| `--size-padding` | 12px | 8px | 内边距 |
| `--size-padding-sm` | 8px | 4px | 小内边距 |

## 二、使用方式

### Tailwind 引用变量
```
h-[var(--size-btn)]                    /* 高度 */
rounded-[var(--radius-btn)]            /* 圆角 */
text-[length:var(--size-font-xs)]      /* 字号 */
gap-[var(--size-gap-sm)]               /* 间距 */
px-[var(--size-padding-sm)]            /* 内边距 */
```

### 紧凑模式切换
根容器 `AppLayout` 在 `layoutMode === "compact"` 时给根 `<div>` 添加 CSS 类 `.compact`，
`.compact` 类在 `globals.css` 中覆盖所有 `--size-*` 变量，无需在组件内写 `isCompact` 三元判断。

### Monaco Editor 字号
Monaco `fontSize` 是数字，无法直接读 CSS 变量。约定：
- 默认模式 = 13
- 紧凑模式 = 12

从 `useUIStore().layoutMode` 获取。

## 三、样式一致性规则

1. **所有**按钮使用 `rounded-[var(--radius-btn)]`，禁止 `rounded-md` / `rounded-lg` 等。
2. **所有**输入框使用 `rounded-[var(--radius-input)]`。
3. **所有**弹窗/面板使用 `rounded-[var(--radius-panel)]`。
4. **所有**右键菜单使用 `rounded-[var(--radius-menu)]`。
5. 不在组件内使用 `isCompact ? "h-6" : "h-7"` 这类硬编码，改用变量。
6. 新增组件**必须**使用设计令牌，不得引入新的硬编码尺寸。

## 四、覆盖范围

以下界面已完成适配，新增界面需同样遵守：

- AppLayout（工具栏、窗口控制、侧边栏、AI面板）
- Sidebar（搜索框、树节点、右键菜单）
- TabBar / TabContent
- DataGrid / DataGridToolbar
- SQLEditor / DDLViewer / MarkdownEditor
- SettingsDialog / GeneralSettings / AISettings
- ConnectionDialog / DatabaseSwitcher
- CommandPalette / AIPanel
- ContextMenu / RowPreview

## 五、拖拽分隔条规范

侧边栏、AI 面板、SQL 编辑器/数据区分割条统一样式：
- 宽/高 5px，`cursor-col-resize` 或 `cursor-row-resize`
- 默认透明，hover 时显示 `--accent` 色半透明线条 + 中心圆点指示器
- 使用 `transition-colors duration-200` / `transition-all duration-200` 实现丝滑动画

## 六、表格与结构编辑专项规范

### 1. 表头与列分隔线

1. 所有数据表格、结构表格、索引表格的表头必须有清晰可见的横向与纵向分隔线。
2. 当 `border-collapse` 导致表头竖线不明显时，应使用更稳定的实现方式（如 inset 阴影或等效方案）确保分隔线可见。
3. 表头分隔线必须与内容区列边界严格对齐，不允许表头与内容列错位。
4. 操作列同样属于表格列体系，表头与内容必须保持统一宽度和分隔规则。

### 2. 固定列布局

1. 结构编辑类表格（如字段、索引、DDL 预览相关表格）必须优先使用固定列布局。
2. 涉及内联编辑的表格必须使用 `tableLayout: fixed` 与 `colgroup` 或等效固定宽度方案。
3. 进入编辑态后，列宽不得因为输入框、图标、按钮、placeholder 或文本内容发生变化。
4. 操作列宽度必须固定，禁止在 hover、编辑态、显示/隐藏按钮时导致列宽抖动。

### 3. 内联编辑规范

1. 内联编辑必须保持行高不变。
2. 内联编辑必须保持列宽不变。
3. 输入框、下拉框、复选框进入编辑态时，必须采用单元格内覆盖式实现，避免参与文档流重新计算布局。
4. 编辑态样式必须与同类区域保持一致，例如字段区与索引区的内联编辑风格必须统一。
5. 新增行、编辑行、选中行的视觉状态必须清晰区分，但不得影响整体表格布局稳定性。
6. placeholder、图标、输入边框的变化不得引起内容跳动或单元格尺寸变化。

### 4. 结构编辑交互一致性

1. 字段区与索引区在交互模型上必须保持一致。
2. 对于新增、删除、撤销、保存等结构编辑操作，按钮位置、触发方式、禁用态、选中态必须统一。
3. 顶部工具栏中的新增/删除操作优先采用统一模式，不允许同类功能一部分使用行内删除、一部分使用顶部删除，除非有明确设计说明。
4. 删除操作默认基于“选中项”执行；若存在新增未提交项，应与已有项删除规则保持一致的心智模型。
5. 新增项的创建/取消按钮可保留在行内，但删除既有项的入口应与同区域主交互保持一致。

### 5. 视觉稳定性规范

1. 任何 hover、focus、active、selected、editing 状态切换，都不得导致布局抖动。
2. 分隔条、工具栏、表头、表格内容区之间不得出现多余双线、白缝或断裂感。
3. 所有结构编辑区域应优先保证“像 TablePlus 一样稳定、紧凑、克制”，避免多余装饰导致的视觉噪音。
4. 对齐、间距、分隔线、背景层级必须在字段区、索引区、数据区之间保持统一语言。

### 6. 工具栏动作规范

1. 工具栏操作顺序必须稳定，常见顺序为：新增 → 删除 → 撤销/刷新/保存。
2. 删除按钮在无选中项时必须禁用，并提供清晰的弱化态。
3. 所有表格工具按钮尺寸、圆角、图标大小必须与全局设计令牌一致。
4. 同一类工具按钮在不同区域应尽量保持一致的 hover、danger、disabled 反馈。

### 7. 最佳实践要求

1. 对结构编辑类界面进行修改时，必须同时检查：
   - 行高是否稳定
   - 列宽是否稳定
   - 表头分隔线是否清晰
   - 字段区与索引区交互是否一致
   - 工具栏按钮位置是否统一
2. 修改表格相关样式后，必须验证默认态、hover 态、选中态、编辑态、新增态、删除态。
3. 若某个实现会在视觉一致性与代码简洁之间冲突，优先保证视觉一致性与交互稳定性。

